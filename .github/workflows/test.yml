name: test
on:
  workflow_dispatch:
    inputs:
      action:
        description: 'Action'
        type: string
      number:
        description: 'Number'
        type: string
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print Test
        run:
          echo ${{ inputs.action }}
          echo ${{ inputs.number }}
