name: test
on:
  repository_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print Test
        run: |
          echo "test"
          echo ${{ fromJSON('{"payload":' . github.event.client_payload.payload . '}') }}